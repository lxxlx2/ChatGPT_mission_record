---
run_time_bangkok: 2026-09-14T04:49:46+07:00
run_time_utc: 2026-09-13T21:49:46Z
automation_id: "6a85fff710e0819190ffcf8c1145a170"
run_status: failed_post_run_qa
candidate_events: 6
triggered_event: true
triggered_project: "Space (@intodotspace)"
triggered_event_type: final_claim_deadline
qa_performed: true
qa_issues_found: 2
qa_corrections: 2
ChatGPT_notification: true
gmail_attempted: true
gmail_sent: true
gmail_subject: "[空投/TGE提醒][Space][最终Claim窗口截止]"
gmail_message_id: "1a09cc0fb4edb6f0"
gmail_error: null
event_report_path: "airdrop-tge-monitor/reports/events/2026/2026-09/2026-09-14/045127-space-final-claim-deadline.md"
event_status: retracted
history_gap_check: passed
identity_registry_checked: false
candidate_source_account: "@spacecoin"
candidate_source_url: "Spacecoin official X announcement/search result"
identity_match: false
identity_conflicts: "source account @spacecoin != whitelist @intodotspace; $SPACE candidate was joined to into.space/$SPC pages"
same_name_collision_checked: false
hard_fail_triggered: false
post_run_correction: true
incident_report: "airdrop-tge-monitor/incidents/2026-09-14-space-cross-project-misattribution.md"
---

# 全项目空投 / TGE / 权益监控运行记录：事后修正

## 1. 最终判定
本轮原先记录为 partial 且触发 Space 正式提醒。经用户质疑后重新核验，判定为跨项目误归属，run_status 更正为 `failed_post_run_qa`。

正式提醒对应 event 已改为 `status: retracted`，不得继续作为事实或去重基线。

## 2. 白名单身份
本轮白名单中的目标项目明确为：

`Space (@intodotspace)`

候选公告的实际来源属于：

`Spacecoin (@spacecoin)`

两者属于不同项目。

## 3. 错误链路
错误流程为：

1）发现 Spacecoin `@spacecoin` 的 `$SPACE` airdrop/final claim 候选。
2）候选进入 `Space (@intodotspace)` 上下文时没有保留来源账号作为 hard identity key。
3）随后独立发现 `into.space`、`migrate.ufogaming.io`、`claim.into.space` 页面。
4）由于名称相似以及存在 `$SPACE` / `$SPC` 字样，把两个项目的信息拼成了伪证据链。
5）QA 已看到 ticker 冲突，却错误采用“通知里不依赖 ticker”继续放行。

## 4. QA 应有行为
一旦发现 `$SPACE` 与 `$SPC`、`@spacecoin` 与 `@intodotspace` 之间存在身份冲突，应立即触发 hard fail：

`identity_match: false`

`hard_fail_triggered: true`

`ChatGPT_notification: false`

`gmail_attempted: false`

不得继续寻找其他同名域名来补足证据链。

## 5. 已执行修复
1）正式 event 已撤销并标记 `status: retracted`。
2）建立事故报告 `airdrop-tge-monitor/incidents/2026-09-14-space-cross-project-misattribution.md`。
3）README 新增 canonical identity tuple、two-anchor rule、same-name collision check 与 hard-fail 规则。
4）后续 run 必须记录 `identity_registry_checked`、`candidate_source_account`、`candidate_source_url`、`identity_match`、`identity_conflicts`、`same_name_collision_checked`、`hard_fail_triggered`。
5）针对 Space 增加回归规则：来自 `@spacecoin` 的任何 `$SPACE` airdrop/claim 信息不得归入 `Space (@intodotspace)`。

## 6. 原提醒影响
已发送的 Gmail message_id `1a09cc0fb4edb6f0` 属于错误提醒。GitHub 历史中保留该 message_id 用于审计，但事件状态已撤销，后续每日汇总不得把它计入有效 triggered event，应单独计入 retracted/false-positive。
